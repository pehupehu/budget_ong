<div class="form-row">
    {{ form_label(formBatchActions.action, null, {
        'label_attr': {
            'class': 'col-sm-1 col-form-label'
        }
    }) }}
    <div class="col-sm-2">
        {{ form_widget(formBatchActions.action, {
            'attr': {
                'class': 'form-control-sm'
            }
        }) }}
    </div>
    <div class="pl-1">
        {{ form_row(formBatchActions.submit, {
            'label': 'generic.batch-actions.go',
            'attr': {
                'class': 'btn-sm btn-primary'
            }
        }) }}
    </div>
</div>
